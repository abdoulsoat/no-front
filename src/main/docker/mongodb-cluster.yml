version: '2'
services:
  nofront-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/nofront-mongodb-config --bind_ip 0.0.0.0
  nofront-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  nofront-mongodb-config:
    image: mongo:4.2.7
    container_name: nofront-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
